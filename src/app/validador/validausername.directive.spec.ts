import { ValidausernameDirective } from './validausername.directive';

describe('ValidausernameDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidausernameDirective();
    expect(directive).toBeTruthy();
  });
});
