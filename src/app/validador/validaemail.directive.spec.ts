import { ValidaemailDirective } from './validaemail.directive';

describe('ValidaemailDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidaemailDirective();
    expect(directive).toBeTruthy();
  });
});
