import { ValidapasswordDirective } from './validapassword.directive';

describe('ValidapasswordDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidapasswordDirective();
    expect(directive).toBeTruthy();
  });
});
