import { ValidadniDirective } from './validadni.directive';

describe('ValidadniDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidadniDirective();
    expect(directive).toBeTruthy();
  });
});
