import { ValidatelefonoDirective } from './validatelefono.directive';

describe('ValidatelefonoDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidatelefonoDirective();
    expect(directive).toBeTruthy();
  });
});
