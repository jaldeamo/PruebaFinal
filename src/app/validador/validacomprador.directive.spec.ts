import { ValidacompradorDirective } from './validacomprador.directive';

describe('ValidacompradorDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidacompradorDirective();
    expect(directive).toBeTruthy();
  });
});
